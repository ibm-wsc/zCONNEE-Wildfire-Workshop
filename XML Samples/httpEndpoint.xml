<server description="basic security">

   <httpEndpoint id="defaultHttpEndpoint"
                 host="${hostName}"
                 httpPort="${httpPort}"
                 httpsPort="${httpsPort}" />

</server>
